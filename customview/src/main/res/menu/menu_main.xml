<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">
    <item android:id="@+id/menu_linear_layout"
          android:title="LinearLayout"
          android:orderInCategory="100"
          app:showAsAction="never"/>
      <item android:id="@+id/menu_grid_view"
            android:title="GridView"
            android:orderInCategory="100"
            app:showAsAction="never"/>
      <item android:id="@+id/menu_hor_grid_view"
            android:title="HorizontalGridView"
            android:orderInCategory="100"
            app:showAsAction="never"/>
      <item android:id="@+id/menu_staggered"
            android:title="StaggerGridView"
            android:orderInCategory="100"
            app:showAsAction="never"/>
</menu>
